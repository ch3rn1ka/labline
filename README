# labline
Labline is a status panel for Wayland compositors, similar to that of dwm. Before installing, make sure that your compositor supports `wlr_layer_shell` and `ext_workspace`. Compile with `make`.